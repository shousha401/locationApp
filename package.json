{
  "name": "location-app",
  "version": "1.0.0",
  "private": true,
  "description": "Live Swarmbox inventory by location, with per-user notes/flags (viewer/editor roles)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "add-user": "node scripts/add-user.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
